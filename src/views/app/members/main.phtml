<?php
if (isset($_SESSION['data'])) {
  $data = $_SESSION['data'];
};
if (isset($this->view->search)) {
  $members = $this->view->search['members'];
  $totalMembers = $this->view->search['totalMembers'];
  $totalPages = $this->view->search['totalPages'];
  $sort = $this->view->search['sort'];
  $page = $this->view->search['page'];
  $limit = $this->view->search['limit'];
};
?>
<div class="container-fluid mb-3">
  <div class="row mb-4">
    <div class="col">
      <p class="fw-bold fs-4 m-0">MEMBERS</p>
      <small class="text-secondary m-0"> Member registration and search </small>
    </div>
  </div>
  <div class="row mb-4">
    <div class="col-12">
      <div class="accordion shadow-sm" id="memberAccordion">
        <div class="accordion-item">
          <h2 class="accordion-header">
            <button
              class="accordion-button collapsed"
              type="button"
              data-bs-toggle="collapse"
              data-bs-target="#memberForm"
              aria-expanded="true"
              aria-controls="memberForm">
              <i class="bi bi-person-plus-fill me-2"></i>
              Register Member
            </button>
          </h2>
          <div
            id="memberForm"
            class="accordion-collapse collapse"
            data-bs-parent="#memberAccordion">
            <div class="accordion-body">
              <form action="/members/create" method="POST">
                <input
                  type="hidden"
                  name="csrf_token"
                  value="<?php echo $this->view->csrf_token; ?>" />
                <div class="row g-3">
                  <div class="col-12 col-md-6">
                    <label for="name" class="form-label"> Name </label>
                    <input
                      type="text"
                      class="form-control"
                      id="name"
                      name="name"
                      value="<?php echo $data['name'] ?? '' ?>"
                      placeholder="Member name"
                      required />
                  </div>
                  <div class="col-12 col-md-6">
                    <label for="email" class="form-label"> Email </label>
                    <input
                      type="email"
                      class="form-control"
                      id="email"
                      name="email"
                      value="<?php echo $data['email'] ?? '' ?>"
                      placeholder="member@email.com"
                      required />
                  </div>
                  <div class="col-12 col-md-6">
                    <label for="phone" class="form-label"> Phone (optional) </label>
                    <input
                      type="text"
                      class="form-control"
                      id="phone"
                      name="phone"
                      value="<?php echo $data['phone'] ?? '' ?>"
                      placeholder="Phone number" />
                  </div>
                  <div class="col-12 col-md-6">
                    <label for="document" class="form-label"> Document (optional) </label>
                    <input
                      type="text"
                      class="form-control"
                      id="document"
                      name="document"
                      value="<?php echo $data['document'] ?? '' ?>"
                      placeholder="Document number" />
                  </div>
                  <div class="col-12 d-flex justify-content-end">
                    <button type="submit" class="btn btn-primary">
                      <i class="bi bi-check-lg me-1"></i>
                      Register Member
                    </button>
                  </div>
                </div>
              </form>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
  <div class="row">
    <div class="col-12">
      <div class="card shadow-sm">
        <div
          class="card-header bg-body d-flex justify-content-between align-items-center">
          <div>
            <h5 class="mb-0">Members</h5>
            <small class="text-body-secondary"> Registered members </small>
          </div>
          <span class="badge text-bg-secondary"><?= htmlspecialchars($totalMembers ?? '0') ?> members</span>
        </div>
        <div class="card-body border-bottom pb-3">
          <form action="/members" method="GET">
            <div class="row g-3">
              <div class="col-12 col-lg-8">
                <label for="search" class="form-label"> Search </label>
                <input
                  type="text"
                  class="form-control"
                  id="search"
                  name="name"
                  value="<?php echo $_GET['name'] ?? '' ?>"
                  placeholder="Enter with the name..." />
              </div>
              <div class="col-12 col-lg-4 d-flex align-items-end gap-2">
                <button type="submit" class="btn btn-primary w-100 d-flex flex-nowrap justify-content-center">
                  <i class="bi bi-search me-1"></i>
                  Search
                </button>
                <a href="/members" class="btn btn-outline-secondary w-100 d-flex flex-nowrap justify-content-center">
                  <i class="bi bi-x-lg me-1"></i>
                  Clear
                </a>
              </div>
            </div>
          </form>
        </div>
        <div class="table-responsive">
          <table class="table table-hover align-middle mb-0">
            <thead>
              <tr>
                <th>Name</th>
                <th>Email</th>
                <th class="d-none d-lg-table-cell">Phone</th>
                <th class="d-none d-lg-table-cell">Document</th>
                <th class="text-end">Options</th>
              </tr>
            </thead>
            <tbody>
              <?php if (empty($members)): ?>
                <tr>
                  <td colspan="6" class="text-center text-body-secondary py-4">
                    No books found.
                  </td>
                </tr>
              <?php else: ?>
                <?php foreach ($members as $member): ?>
                  <tr>
                    <td class="text-capitalize"><?= htmlspecialchars($member->__get('name') ?? '—') ?></td>
                    <td><?= htmlspecialchars($member->__get('email') ?? '—') ?></td>
                    <td class="d-none d-lg-table-cell"><?= htmlspecialchars($member->__get('phone') ?? '0') ?></td>
                    <td class="d-none d-lg-table-cell"><?= htmlspecialchars($member->__get('document') ?? '0') ?></td>
                    <td class="text-end">
                      <div class="dropdown">
                        <button
                          class="btn btn-sm btn-outline-secondary dropdown-toggle"
                          type="button"
                          data-bs-toggle="dropdown"
                          aria-expanded="false">
                          Options
                        </button>
                        <ul class="dropdown-menu dropdown-menu-end">
                          <li>
                            <a
                              class="dropdown-item"
                              href="/members/edit/<?= htmlspecialchars($member->__get('code')) ?>">
                              <i class="bi bi-pencil me-2"></i>
                              Edit
                            </a>
                          </li>
                          <li>
                            <form
                              action="/members/delete/<?= htmlspecialchars($member->__get('code')) ?>"
                              method="POST"
                              data-confirm="The book will be deleted. Are you sure?">
                              <input
                                type="hidden"
                                name="csrf_token"
                                value="<?= $this->view->csrf_token ?>" />
                              <button class="dropdown-item text-danger">
                                <i class="bi bi-trash3-fill me-2"></i>
                                Delete
                              </button>
                            </form>
                          </li>
                        </ul>
                      </div>
                    </td>
                  </tr>
                <?php endforeach; ?>
              <?php endif; ?>
            </tbody>
          </table>
        <?php if ($totalPages > 0): ?>
          <nav class="mt-4" aria-label="Books pagination">
            <ul class="pagination justify-content-center">
              <li class="page-item <?= $page <= 1 ? 'disabled' : '' ?>">
                <a
                  class="page-link"
                  href="?page=<?= $page - 1 ?>&sort=<?= htmlspecialchars($sort) ?>">
                  Previous
                </a>
              </li>
              <?php for ($i = 1; $i <= $totalPages; $i++): ?>
                <li class="page-item <?= $i === $page ? 'active' : '' ?>">
                  <a
                    class="page-link"
                    href="?page=<?= $i ?>&sort=<?= htmlspecialchars($sort) ?>">
                    <?= $i ?>
                  </a>
                </li>
              <?php endfor; ?>
              <li class="page-item <?= $page >= $totalPages ? 'disabled' : '' ?>">
                <a
                  class="page-link"
                  href="?page=<?= $page + 1 ?>&sort=<?= htmlspecialchars($sort) ?>">
                  Next
                </a>
              </li>
            </ul>
          </nav>
        <?php endif; ?>
        </div>
      </div>
    </div>
  </div>
</div>
<?php
unset($_SESSION['data']);
unset($_SESSION['error']);
unset($_SESSION['success']);
?>
