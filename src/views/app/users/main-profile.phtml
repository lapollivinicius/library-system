<div class="container-fluid mb-3">
  <div class="row mb-4">
    <div class="col">
      <p class="fw-bold fs-4 m-0">PROFILE</p>
      <small class="text-secondary m-0">Manage your account details and preferences</small>
    </div>
  </div>
  <form 
    action="/profile/update" 
    method="POST" 
    data-confirm="The data will be modified when you click Save. Are you sure?"
  >
    <input type="hidden" name="csrf_token" value="<?php echo $this->view->csrf_token; ?>" />
    <div class="row g-4 mb-4">
      <div class="col-12">
        <div class="border rounded p-3 h-100">
          <label for="userName" class="form-label text-body-secondary">
            Library Name
          </label>
          <input
            type="text"
            class="form-control text-capitalize"
            id="userName"
            name="library_name"
            value="<?php echo htmlspecialchars($_SESSION['library_name'] ?? '') ?>"
            style="border: 1px solid transparent"
            required />
        </div>
      </div>
      <div class="col-12 col-md-6">
        <div class="border rounded p-3 h-100">
          <label for="userEmail" class="form-label text-body-secondary">
            Email
          </label>
          <input
            type="email"
            class="form-control disabled"
            id="userEmail"
            name="email"
            placeholder="u••••@gmail.com"
            value="<?php echo htmlspecialchars($_SESSION['email'] ?? '') ?>"
            style="border: 1px solid transparent"
            disabled />
        </div>
      </div>
      <div class="col-12 col-md-6">
        <div class="border rounded p-3 h-100">
          <label for="userPassword" class="form-label text-body-secondary">
            Name
          </label>
          <input
            type="text"
            class="form-control disabled"
            id="userName"
            name="name"
            placeholder="<?php echo htmlspecialchars($_SESSION['name'] ?? '') ?>"
            style="border: 1px solid transparent"
            disabled />
        </div>
      </div>
      <div class="col-12 d-flex justify-content-end gap-2">
        <button type="submit" class="btn btn-success" id="saveProfileButton">
          <i class="bi bi-check-lg me-1"></i>
          Save Changes
        </button>
      </div>
    </div>
  </form>
  <div class="row mt-5 mb-4">
    <div class="col">
      <h5 class="mb-0 text-danger">Danger Zone</h5>
      <small class="text-body-secondary">Irreversible account actions</small>
    </div>
  </div>
  <div class="row g-4">
    <div class="col-12">
      <div class="border border-danger rounded p-4 d-flex flex-column flex-md-row justify-content-between align-items-md-center gap-3">
        <div>
          <h6 class="mb-1 text-danger">Delete Account</h6>
          <small class="text-body-secondary">Once you delete your account, there is no going back. Please be certain.</small>
        </div>
        <form action="/profile/delete" method="POST" data-confirm="This action is irreversible. Are you absolutely sure you want to delete your account?">
          <input type="hidden" name="csrf_token" value="<?php echo $this->view->csrf_token; ?>" />
          <button type="submit" class="btn btn-outline-danger w-100">
            <i class="bi bi-trash me-1"></i>
            Delete Account
          </button>
        </form>
      </div>
    </div>
  </div>
</div>
<?php
unset($_SESSION['data']);
unset($_SESSION['error']);
unset($_SESSION['success']);
?>
