<?php
if (isset($_SESSION['data'])) {
  $data = $_SESSION['data'];
};

if (isset($this->view->search)) {
  $books = $this->view->search['books'];
  $totalBooks = $this->view->search['totalBooks'];
  $totalPages = $this->view->search['totalPages'];
  $sort = $this->view->search['sort'];
  $page = $this->view->search['page'];
  $limit = $this->view->search['limit'];
};

?>
<div class="container-fluid mb-3">
  <div class="row mb-4">
    <div class="col">
      <p class="fw-bold fs-4 m-0">BOOKS</p>
      <small class="text-secondary m-0"> Book registration and search </small>
    </div>
  </div>
  <div class="row mb-4">
    <div class="col-12">
      <div class="accordion shadow-sm" id="bookAccordion">
        <div class="accordion-item">
          <h2 class="accordion-header">
            <button
              class="accordion-button collapsed"
              type="button"
              data-bs-toggle="collapse"
              data-bs-target="#bookForm"
              aria-expanded="true"
              aria-controls="bookForm">
              <i class="bi bi-plus-circle-fill me-2"></i>
              Register Book
            </button>
          </h2>
          <div
            id="bookForm"
            class="accordion-collapse collapse"
            data-bs-parent="#bookAccordion">
            <div class="accordion-body">
              <form action="/books/create" method="POST">
                <input
                  type="hidden"
                  name="csrf_token"
                  value="<?php echo $this->view->csrf_token; ?>" />
                <div class="row g-3">
                  <div class="col-12 col-md-6 position-relative">
                    <label for="title" class="form-label">Title</label>

                    <input
                      type="text"
                      class="form-control"
                      id="title"
                      name="title"
                      value="<?php echo $data['title'] ?? '' ?>"
                      placeholder="Book title"
                      autocomplete="off"
                      data-autocomplete-url="/api/books"
                      data-autocomplete-search-param="title"
                      data-autocomplete-target="book-title-results"
                      data-autocomplete-min="2"
                      aria-autocomplete="list"
                      aria-expanded="false"
                      required />

                    <div
                      id="book-title-results"
                      class="position-absolute start-0 end-0 text-bg-dark border rounded-3 shadow-sm mt-1 overflow-auto"
                      style="max-height: 300px; z-index: 1050"
                      role="listbox"
                      hidden>
                      <button
                        type="button"
                        class="dropdown-item p-3 border-bottom"
                        data-autocomplete-option="Clean Code"
                        data-value="15"
                        data-label="Clean Code">
                        <div class="fw-semibold">Clean Code</div>
                        <small class="text-body-secondary">
                          Robert C. Martin
                        </small>
                      </button>
                    </div>
                  </div>

                  <div class="col-12 col-md-6">
                    <label for="author" class="form-label"> Author </label>
                    <input
                      type="text"
                      class="form-control"
                      id="author"
                      name="author"
                      value="<?php echo $data['author'] ?? '' ?>"
                      placeholder="Author name"
                      required />
                  </div>
                  <div class="col-12 col-md-4">
                    <label for="year" class="form-label"> Year </label>
                    <input
                      type="number"
                      class="form-control"
                      id="year"
                      name="year"
                      value="<?php echo $data['year'] ?? '' ?>"
                      placeholder="2026"
                      min="0"
                      required />
                  </div>
                  <div class="col-12 col-md-4">
                    <label for="genre" class="form-label"> Genre </label>
                    <input
                      type="text"
                      class="form-control"
                      id="genre"
                      name="genre"
                      value="<?php echo $data['genre'] ?? '' ?>"
                      placeholder="Genre"
                      required />
                  </div>
                  <div class="col-12 col-md-4">
                    <label for="quantity" class="form-label"> Quantity </label>
                    <input
                      type="number"
                      class="form-control"
                      id="quantity"
                      name="quantity"
                      value="<?php echo $data['quantity'] ?? '' ?>"
                      placeholder="0"
                      min="1"
                      required />
                  </div>
                  <div class="col-12">
                    <label for="isbn" class="form-label"> ISBN (Optional) </label>
                    <input
                      type="text"
                      class="form-control"
                      id="isbn"
                      name="isbn"
                      value="<?php echo $data['isbn'] ?? '' ?>"
                      placeholder="ISBN" />
                  </div>

                  <?php if (isset($_SESSION['success'])) { ?>
                    <div class="alert alert-success d-flex align-items-center gap-2" role="alert">
                      <i class="bi bi-check-circle-fill"></i>
                      <p class="mb-0"><?php echo $_SESSION['success'] ?></p>
                    </div>
                  <?php } ?>

                  <?php if (isset($_SESSION['error'])) { ?>
                    <div class="alert alert-danger d-flex align-items-center gap-2" role="alert">
                      <i class="bi bi-exclamation-circle"></i>
                      <p class="mb-0"><?php echo $_SESSION['error'] ?></p>
                    </div>
                  <?php } ?>

                  <div class="col-12 d-flex justify-content-end">
                    <button type="submit" class="btn btn-primary">
                      <i class="bi bi-check-lg me-1"></i>
                      Register Book
                    </button>
                  </div>
                </div>
              </form>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
  <div class="row">
    <div class="col-12">
      <div class="card shadow-sm">
        <div
          class="card-header bg-body d-flex justify-content-between align-items-center">
          <div>
            <h5 class="mb-0">Search Results</h5>
            <small class="text-body-secondary"> Registered books </small>
          </div>
          <span class="badge text-bg-secondary"> <?php echo $totalBooks ?? 0 ?> books </span>
        </div>
        <div class="card-body border-bottom pb-3">
          <form action="/books" method="GET">
            <div class="row g-3">

              <div class="col-12 col-md-9">
                <label for="search" class="form-label"> Search </label>
                <input
                  type="text"
                  class="form-control"
                  id="search"
                  name="title"
                  value="<?php echo $_GET['search'] ?? '' ?>"
                  placeholder="Enter with the title..." />
              </div>

              <div class="col-12 col-md-3 d-flex align-items-end gap-2">
                <button type="submit" class="btn btn-primary w-100">
                  <i class="bi bi-search me-1"></i>
                  Search
                </button>
                <a href="/books" class="btn btn-outline-secondary w-100">
                  <i class="bi bi-x-lg me-1"></i>
                  Clear
                </a>
              </div>

            </div>
          </form>
        </div>
        <div class="table-responsive">
          <table class="table table-hover align-middle mb-0">
            <thead>
              <tr>
                <th>#</th>
                <th>Title</th>
                <th class="d-none d-md-table-cell">Author</th>
                <th class="d-none d-md-table-cell">Year</th>
                <th class="d-none d-md-table-cell">Quant</th>
                <!-- <th class="d-none d-md-table-cell">Status</th> -->
                <th  class="text-end">Options</th>
              </tr>
            </thead>
            <tbody>

              <?php if (empty($books)): ?>
                <tr>
                  <td colspan="6" class="text-center text-body-secondary py-4">
                    No books found.
                  </td>
                </tr>
              <?php else: ?>
                <?php foreach ($books as $book): ?>
                  <tr>
                    <td><?= htmlspecialchars($book->__get('code')) ?></td>
                    <td><?= htmlspecialchars($book->__get('title')) ?></td>
                    <td class="d-none d-md-table-cell"><?= htmlspecialchars($book->__get('author')) ?></td>
                    <td class="d-none d-md-table-cell"><?= htmlspecialchars($book->__get('year')) ?></td>
                    <td class="d-none d-md-table-cell"><?= htmlspecialchars($book->__get('quantity')) ?></td>

                    <!-- <td class="d-none d-md-table-cell">
                      <?php if ($book->__get('is_active')): ?>
                        <span class="badge text-bg-success">
                          Active
                        </span>
                      <?php else: ?>
                        <span class="badge text-bg-secondary">
                          Inactive
                        </span>
                      <?php endif; ?>
                    </td> -->

                    <td class="text-end">
                      <div class="dropdown " >
                        <button
                          class="btn btn-sm btn-outline-secondary dropdown-toggle"
                          type="button"
                          data-bs-toggle="dropdown"
                          aria-expanded="false">
                          Options
                        </button>
                        <ul class="dropdown-menu dropdown-menu-end">
                          <li>
                            <a
                              class="dropdown-item"
                              href="/books/edit/<?= htmlspecialchars($book->__get('code')) ?>"
                              onclick="event.stopPropagation()">
                              <i class="bi bi-pencil me-2"></i>
                              Edit
                            </a>
                          </li>
                          <li>
                            <form
                              action="/books/delete/<?= htmlspecialchars($book->__get('code')) ?>"
                              method="POST"
                              data-confirm="The book will be deleted. Are you sure?">
                              <input
                                type="hidden"
                                name="csrf_token"
                                value="<?= $this->view->csrf_token ?>" />
                              <button class="dropdown-item">
                                <i class="bi bi-trash3-fill me-2"></i>
                                Delete
                              </button>
                            </form>
                          </li>
                        </ul>
                      </div>
                    </td>
                  </tr>
                <?php endforeach; ?>
              <?php endif; ?>
            </tbody>
          </table>

          <?php if ($totalPages > 0): ?>
            <nav class="mt-4" aria-label="Books pagination">
              <ul class="pagination justify-content-center">
                <li class="page-item <?= $page <= 1 ? 'disabled' : '' ?>">
                  <a
                    class="page-link"
                    href="?page=<?= $page - 1 ?>&sort=<?= htmlspecialchars($sort) ?>">
                    Previous
                  </a>
                </li>
                <?php for ($i = 1; $i <= $totalPages; $i++): ?>
                  <li class="page-item <?= $i === $page ? 'active' : '' ?>">
                    <a
                      class="page-link"
                      href="?page=<?= $i ?>&sort=<?= htmlspecialchars($sort) ?>">
                      <?= $i ?>
                    </a>
                  </li>
                <?php endfor; ?>
                <li class="page-item <?= $page >= $totalPages ? 'disabled' : '' ?>">
                  <a
                    class="page-link"
                    href="?page=<?= $page + 1 ?>&sort=<?= htmlspecialchars($sort) ?>">
                    Next
                  </a>
                </li>
              </ul>
            </nav>
          <?php endif; ?>

        </div>
      </div>
    </div>
  </div>
</div>
<?php
unset($_SESSION['data']);
unset($_SESSION['error']);
unset($_SESSION['success']);
?>