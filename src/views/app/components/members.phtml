<div class="container-fluid mb-3">
  <div class="row mb-4">
    <div class="col">
      <p class="fw-bold fs-4 m-0">MEMBERS</p>
      <small class="text-secondary m-0"> Member registration and search </small>
    </div>
  </div>
  <div class="row mb-4">
    <div class="col-12">
      <div class="accordion shadow-sm" id="memberAccordion">
        <div class="accordion-item">
          <h2 class="accordion-header">
            <button
              class="accordion-button collapsed"
              type="button"
              data-bs-toggle="collapse"
              data-bs-target="#memberForm"
              aria-expanded="true"
              aria-controls="memberForm"
            >
              <i class="bi bi-person-plus-fill me-2"></i>
              Register Member
            </button>
          </h2>
          <div
            id="memberForm"
            class="accordion-collapse collapse"
            data-bs-parent="#memberAccordion"
          >
            <div class="accordion-body">
              <form action="/members" method="POST">
                <div class="row g-3">
                  <!-- NAME -->
                  <div class="col-12 col-md-6">
                    <label for="name" class="form-label"> Name </label>

                    <input
                      type="text"
                      class="form-control"
                      id="name"
                      name="name"
                      placeholder="Member name"
                      required
                    />
                  </div>
                  <div class="col-12 col-md-6">
                    <label for="email" class="form-label"> Email </label>

                    <input
                      type="email"
                      class="form-control"
                      id="email"
                      name="email"
                      placeholder="member@email.com"
                      required
                    />
                  </div>
                  <div class="col-12 col-md-6">
                    <label for="phone" class="form-label"> Phone </label>

                    <input
                      type="text"
                      class="form-control"
                      id="phone"
                      name="phone"
                      placeholder="Phone number"
                      required
                    />
                  </div>
                  <div class="col-12 col-md-6">
                    <label for="document" class="form-label"> Document </label>

                    <input
                      type="text"
                      class="form-control"
                      id="document"
                      name="document"
                      placeholder="Document number"
                      required
                    />
                  </div>
                  <div class="col-12 d-flex justify-content-end">
                    <button type="submit" class="btn btn-primary">
                      <i class="bi bi-check-lg me-1"></i>
                      Register Member
                    </button>
                  </div>
                </div>
              </form>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
  <div class="row">
    <div class="col-12">
      <div class="card shadow-sm">
        <div
          class="card-header bg-body d-flex justify-content-between align-items-center"
        >
          <div>
            <h5 class="mb-0">Members</h5>
            <small class="text-body-secondary"> Registered members </small>
          </div>
          <span class="badge text-bg-secondary"> 00 members </span>
        </div>
        <div class="card-body border-bottom pb-3">
          <form action="/members" method="GET">
            <div class="row g-3">
              <div class="col-12 col-md-4">
                <label for="searchField" class="form-label"> Search by </label>
                <select class="form-select" id="searchField" name="field">
                  <option value="name">Name</option>
                  <option value="email">Email</option>
                  <option value="phone">Phone</option>
                  <option value="document">Document</option>
                </select>
              </div>
              <div class="col-12 col-md-6">
                <label for="search" class="form-label"> Search </label>
                <input
                  type="text"
                  class="form-control"
                  id="search"
                  name="search"
                  placeholder="Enter your search..."
                />
              </div>
              <div class="col-12 col-md-2 d-flex align-items-end">
                <button type="submit" class="btn btn-primary w-100">
                  <i class="bi bi-search me-1"></i>
                  Search
                </button>
              </div>
            </div>
          </form>
        </div>
        <div class="table-responsive">
          <table class="table table-hover align-middle mb-0">
            <thead>
              <tr>
                <th>Name</th>
                <th>Email</th>
                <th>Phone</th>
                <th>Document</th>
                <th>Status</th>
                <th class="text-end">Options</th>
              </tr>
            </thead>
            <tbody>

              <?php foreach ($members as $member): ?>
                <tr>
                  <td><?= htmlspecialchars($member->__get('name') ?? '—') ?></td>
                  <td><?= htmlspecialchars($member->__get('email') ?? '—') ?></td>
                  <td><?= htmlspecialchars($member->__get('phone') ?? '—') ?></td>
                  <td><?= htmlspecialchars($member->__get('document') ?? '—') ?></td>
                  <td>
                    <?php if ($member->__get('has_loan')): ?>
                      <span class="badge text-bg-warning">With Loan</span>
                    <?php else: ?>
                      <span class="badge text-bg-success">No Loan</span>
                    <?php endif; ?>
                  </td>
                  <td class="text-end">
                    <div class="dropdown">
                      <button
                        class="btn btn-sm btn-outline-secondary dropdown-toggle"
                        type="button"
                        data-bs-toggle="dropdown"
                        aria-expanded="false"
                      >
                        Options
                      </button>
                      <ul class="dropdown-menu dropdown-menu-end">
                        <li>
                          <a
                            class="dropdown-item"
                            href="/members/edit/<?= htmlspecialchars($member->__get('id')) ?>"
                          >
                            <i class="bi bi-pencil me-2"></i>
                            Edit
                          </a>
                        </li>
                      </ul>
                    </div>
                  </td>
                </tr>
              <?php endforeach; ?>

              <tr>
                <td colspan="6" class="text-center text-body-secondary py-4">
                  No members found.
                </td>
              </tr>
            </tbody>
          </table>
        </div>
      </div>
    </div>
  </div>
</div>
